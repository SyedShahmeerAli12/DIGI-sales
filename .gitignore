# Secrets — never commit
.env
.env.local
.env.*.local
backend/.env
frontend/.env.local

# Node
node_modules/
.next/
npm-debug.log*

# Python
__pycache__/
*.pyc
.venv/

# Runtime-generated, not a tracked asset — recreated fresh on each deploy
backend/data/chat_history.db

# TypeScript incremental build cache
frontend/tsconfig.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Local tooling artifacts
.claude/
