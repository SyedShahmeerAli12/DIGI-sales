# Used by docker-compose.yml to bake the backend URL into the frontend build.
# On the VPS, set this to the server's public IP or domain, e.g. http://your-vps-ip:8000
BACKEND_PUBLIC_URL=http://localhost:8000
